# Infrastructure

This directory is reserved for deployment runbooks and infrastructure notes that will be added in later phases.

The initial Render Blueprint is tracked at the repository root in `render.yaml`, because Render discovers Blueprints from that location.

Planned contents:

- Docker definitions
- Render deployment and rollback runbooks
- Managed service and secret management references
- CI/CD deployment workflows
- Monitoring and alerting setup notes
